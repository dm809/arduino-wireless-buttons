/*
 * ПРОВЕРОЧНАЯ ПРОШИВКА для калибровки беспроводных кнопок
 * 
 * Подключение: 
 * - Сигнальный провод от блока кнопок к пину A0
 * - GND блока кнопок к GND Arduino
 * 
 * Инструкция:
 * 1. Загрузить эту прошивку в Arduino
 * 2. Открыть Serial Monitor (скорость 9600)
 * 3. По очереди нажимать кнопки пульта
 * 4. Записать значения Raw для каждой кнопки
 * 5. Использовать записанные значения в основной прошивке
 * 
 * Автор: [Твой канал]
 */

void setup() {
  Serial.begin(9600);
  Serial.println("=================================");
  Serial.println("    КАЛИБРОВКА КНОПОК ПУЛЬТА    ");
  Serial.println("=================================");
  Serial.println("Подключи сигнальный провод к A0");
  Serial.println("Нажимай кнопки и записывай значения:");
  Serial.println("");
}

void loop() {
  int rawValue = analogRead(A0);
  float voltage = rawValue * (5.0 / 1023.0);
  
  // Показываем значения только при нажатии кнопок
  if (rawValue > 50) {  // Игнорируем шум
    Serial.print("Raw значение: ");
    Serial.print(rawValue);
    Serial.print(" | Напряжение: ");
    Serial.print(voltage, 2);
    Serial.println("В");
    delay(300);  // Защита от спама
  }
  
  delay(50);
}
